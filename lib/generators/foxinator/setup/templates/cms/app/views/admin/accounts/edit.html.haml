%h2= t('.account')

= comfy_form_for [current_namespace, resource] do |form|
  = render 'admin/admins/form', form: form
  = form.form_group class: 'form-actions' do
    = form.submit t(resource.new_record? ? 'create' : 'update'), class: 'btn btn-primary'
    = link_to t('cancel'), [current_namespace, current_parent, resource.new_record? ? resource_class : resource], class: 'btn btn-link'
